/**
 * @readonly
 * @enum
 */
const ACCESS_TYPE = {
  PUBLIC: 'public',
  PRIVATE: 'private',
};

/**
 * @readonly
 * @enum
 */
const STATUS_TYPE = {
  DRAFT: 'draft',
  PUBLISHED: 'published',
};

module.exports = { ACCESS_TYPE, STATUS_TYPE };
